# Normally this file would be excluded from the git history, and developers would need to duplicate/rename `.env.dist`. 
# But for the purpose simplicity in running this demo i've left it in.

PROVIDER_HOSTNAME=localhost
PROVIDER_PORT=8282
